"""Answer questions against indexed transcripts."""


def answer_question(question: str) -> str:
    """Run retrieval and generation; pipeline wiring is intentionally explicit."""
    del question
    raise NotImplementedError("Connect retrieval and generation components first")
